package TMobile.stepdefs.UCCStepdefs.channelStepdefs;

import cucumber.api.java8.En;

public class ChannelStepdefs implements En {
    public ChannelStepdefs() {
        Then("^I get to the channels page$", () -> {

        });
    }
}
